{
	"name": "Rounding_decimal",
	"properties": {
		"activities": [
			{
				"name": "Set variable1",
				"type": "SetVariable",
				"dependsOn": [],
				"policy": {
					"secureOutput": false,
					"secureInput": false
				},
				"userProperties": [],
				"typeProperties": {
					"variableName": "Number",
					"value": {
						"value": "@concat(split(string(mul(float(pipeline().parameters.Actualnumber), float(pipeline().parameters.decimal_floater))), '.')[0], '.', substring(split(string(mul(float(pipeline().parameters.Actualnumber), float(pipeline().parameters.decimal_floater))), '.')[1], 0, 3))\n",
						"type": "Expression"
					}
				}
			}
		],
		"parameters": {
			"Actualnumber": {
				"type": "float"
			},
			"decimal_floater": {
				"type": "int"
			}
		},
		"variables": {
			"Number": {
				"type": "String"
			}
		},
		"annotations": []
	}
}